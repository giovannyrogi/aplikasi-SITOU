# Database Schema SITOU

Dokumen ini adalah peta cepat database SITOU. Gunakan dokumen ini sebelum membuka `sitou_schema_v3.sql`; buka SQL hanya saat perlu detail constraint, index, fungsi partisi, definisi view, atau membuat migration.

## Prinsip Umum

- `organization_id` adalah batas organisasi utama pada semua tabel bisnis.
- Histori pegawai tidak ditimpa: kontrak ada di `employment_contracts`, penempatan/rolling ada di `employee_assignments`, dan event absensi mentah ada di `attendance_events`.
- File privat tidak pernah dikirim sebagai `object_key` langsung ke browser. Metadata file ada di `stored_files`, termasuk `employee_id` atau `onboarding_draft_id` untuk otorisasi pemilik.
- Event absensi mentah append-only. Rekap cepat dashboard ada di `attendance_daily_summaries` dan boleh dihitung ulang.
- Role stabil di tabel `roles.code`: `superadmin`, `hrd`, `leader`, dan `employee`.

## Organisasi, File, Branding, Struktur

### Alur onboarding dan masa akses

1. Superadmin membuat identitas `organizations` dan periode pertama `organization_subscriptions` dalam satu transaksi.
2. Superadmin atau Admin/HRD menyiapkan `organization_unit_types` sebelum membuat struktur pada `organization_units`.
3. Superadmin membuat minimal satu `locations` yang aktif dan berada dalam umur operasional.
4. Superadmin membuat kredensial `users`, memasang role `hrd`, menautkan profil pegawai bila tersedia, lalu menetapkan cakupan lokasi.
5. Organisasi siap digunakan ketika status administratif aktif, memiliki langganan efektif `active`/`grace`, lokasi operasional, dan Admin/HRD aktif.

`organizations.is_active=false` adalah penghentian administratif manual. Status langganan efektif dihitung dengan timezone organisasi: `scheduled`, `active`, `grace`, atau `expired`; status manual `suspended` dan `cancelled` selalu mengalahkan tanggal. Job menyelaraskan status tersimpan, tetapi login dan validasi session tetap memeriksa tanggal. Perpanjangan selalu menambah baris baru dan periode non-suspended/non-cancelled tidak boleh tumpang tindih.

Superadmin dapat menonaktifkan lokasi tanpa menghapus `user_location_scopes`, profil pegawai, atau histori `employee_assignments`. Login HRD tidak bergantung pada lokasi scope; HRD tetap dapat masuk selama akun, role, organisasi, dan langganan efektif masih aktif. `user_organization_roles.location_scope_mode` menyatakan `all` atau `selected` secara eksplisit agar scope kosong tidak berubah menjadi akses penuh. Pimpinan selalu membaca seluruh data organisasi dalam mode read-only. Akun HRD atau Pimpinan boleh dibuat sebagai akun akses tanpa relasi `employees.user_id`, lalu ditautkan ke profil pegawai bila diperlukan. Akun Pegawai wajib tertaut ke profil pegawai; aksesnya mengikuti profil `employees` dan penempatan utama aktif pada `employee_assignments`, bukan `user_location_scopes`.

Data Master menggunakan route yang sama untuk Superadmin dan Admin/HRD. Superadmin dapat memilih organisasi secara eksplisit, sedangkan API selalu mengganti atau memvalidasi `organization_id` Admin/HRD dari session server. Admin/HRD dapat mengelola `locations`, `organization_unit_types`, `organization_units`, `organization_unit_locations`, `positions`, dan `employment_types` hanya pada organisasinya. Identitas organisasi, masa akses, serta pembuatan Admin/HRD pertama tetap menjadi kewenangan Superadmin. Permission Pegawai memakai kode `*_self` terpisah dan belum membuka endpoint administrasi organisasi; endpoint self-service kelak wajib memperoleh `employee_id` dari session.

### Matriks validasi login dan session

Validasi berikut dijalankan saat login dan diulang ketika session dipakai pada server:

| Kondisi                                                                                               | Superadmin | Admin/HRD | Pimpinan | Pegawai | Kode penolakan login                                             |
| ----------------------------------------------------------------------------------------------------- | ---------- | --------- | -------- | ------- | ---------------------------------------------------------------- |
| `users.is_active=false`                                                                               | Ditolak    | Ditolak   | Ditolak  | Ditolak | `ACCOUNT_INACTIVE`                                               |
| Role belum mulai atau sudah berakhir                                                                  | Ditolak    | Ditolak   | Ditolak  | Ditolak | `ROLE_INACTIVE`                                                  |
| `organizations.is_active=false`                                                                       | N/A        | Ditolak   | Ditolak  | Ditolak | `ORGANIZATION_INACTIVE`                                          |
| Langganan tidak efektif (`scheduled`, `expired`, `suspended`, `cancelled`, atau tidak ada)            | N/A        | Ditolak   | Ditolak  | Ditolak | Kode `ORGANIZATION_*`/`SUBSCRIPTION_*`                           |
| Profil pegawai tidak tersedia, soft-deleted, atau `employment_status` bukan `active` atau `probation` | N/A        | N/A       | N/A      | Ditolak | `EMPLOYEE_PROFILE_INACTIVE`/`EMPLOYEE_STATUS_INACTIVE`           |
| Tidak ada penempatan utama yang efektif pada tanggal berjalan                                         | N/A        | N/A       | N/A      | Ditolak | `EMPLOYEE_ASSIGNMENT_INACTIVE`                                   |
| Lokasi penempatan `is_active=false` atau di luar `operational_from`/`operational_until`               | N/A        | N/A       | N/A      | Ditolak | `EMPLOYEE_LOCATION_INACTIVE`/`EMPLOYEE_LOCATION_NOT_OPERATIONAL` |
| Divisi/unit penempatan `organization_units.is_active=false`                                           | N/A        | N/A       | N/A      | Ditolak | `EMPLOYEE_UNIT_INACTIVE`                                         |

Pesan penolakan tidak membocorkan SQL atau data internal. Akun, profil, penempatan, lokasi, atau divisi yang tidak aktif mengarahkan pengguna ke Admin organisasi. Organisasi atau masa berlaku yang tidak aktif mengarahkan pengguna ke Admin SITOU.

| Tabel                         | Fungsi                                           | Kolom Kunci                                                                                                                                                                                                          |
| ----------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `organizations`               | Identitas organisasi.                            | `id`, `parent_id`, `code`, `name`, `legal_name`, `organization_type`, `timezone`, `locale`, `is_active`, `settings`, audit waktu.                                                                                    |
| `organization_subscriptions`  | Histori periode akses dan masa tenggang.         | `organization_id`, `starts_on`, `ends_on`, `grace_ends_on`, `status`, `notes`, `created_by_user_id`, `request_id`.                                                                                                   |
| `stored_files`                | Metadata file privat dan staging draft pegawai.  | `organization_id`, `employee_id`, `onboarding_draft_id`, `storage_provider`, `object_key`, `original_name`, `mime_type`, `size_bytes`, `sha256`, `category`, `is_confidential`, `uploaded_by_user_id`, `deleted_at`. |
| `organization_branding`       | Logo dan warna organisasi.                       | `organization_id`, `logo_file_id`, `primary_color`, `secondary_color`, `updated_at`.                                                                                                                                 |
| `locations`                   | Kantor pusat, cabang, pasar, site, gudang.       | `organization_id`, `parent_location_id`, `code`, `name`, `location_type`, koordinat, `logo_file_id`, `operational_from`, `operational_until`, `is_active`.                                                           |
| `organization_unit_types`     | Master jenis unit yang fleksibel per organisasi. | `organization_id`, `code`, `name`, `description`, `sort_order`, `is_active`.                                                                                                                                         |
| `organization_units`          | Struktur organisasi bertingkat.                  | `organization_id`, `parent_unit_id`, `code`, `name`, `unit_type_id`, `is_active`.                                                                                                                                    |
| `organization_unit_locations` | Relasi many-to-many unit dan lokasi.             | `organization_id`, `organization_unit_id`, `location_id`, `is_primary`, `active_from`, `active_until`.                                                                                                               |
| `positions`                   | Master jabatan.                                  | `organization_id`, `code`, `name`, `grade`, `level_no`, `is_managerial`, `is_active`.                                                                                                                                |

`organization_unit_types` adalah sumber kebenaran klasifikasi struktur. Nama jenis tidak disimpan sebagai teks bebas pada `organization_units`; setiap unit wajib memakai `unit_type_id` milik organisasi yang sama. Jenis aktif dapat dipilih untuk unit baru, sedangkan jenis yang sudah digunakan harus dinonaktifkan alih-alih dihapus agar histori tetap utuh. `parent_unit_id` mengatur hierarki dan `organization_unit_locations` mengatur lokasi operasional, sehingga keduanya tidak boleh digantikan oleh jenis unit.

`organization_unit_locations.active_from` wajib dikirim eksplisit untuk setiap lokasi. Form menerapkan satu tanggal bersama pada lokasi baru dan tetap mengizinkan override per lokasi. Tanggal harus berada dalam umur operasional lokasi, tidak boleh melewati hari ini, dan periode untuk kombinasi organisasi-unit-lokasi tidak boleh overlap. Koreksi atau pelepasan relasi lama memerlukan alasan, audit, serta pemeriksaan agar histori `employee_assignments` tetap valid.

API master jenis unit menggunakan `GET/POST /api/organization-unit-types`, `GET/PATCH/DELETE /api/organization-unit-types/:id`, dan `GET /api/organization-unit-types/options`. Kode dikunci setelah jenis dipakai; endpoint `DELETE` hanya menonaktifkan. Form Divisi & Unit mengirim `unitTypeId` dan backend selalu memvalidasi jenis aktif serta organisasi pemiliknya.

## User, Role, Permission

| Tabel                     | Fungsi                                                                   | Kolom Kunci                                                                                                          |
| ------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| `users`                   | Kredensial login global tanpa duplikasi identitas.                       | `username`, `password_hash`, `is_active`, `credential_version`, `last_login_at`, `last_login_ip`.                    |
| `platform_user_profiles`  | Identitas Superadmin/platform.                                           | `user_id`, `full_name`, `email`, `whatsapp`, audit waktu.                                                            |
| `v_user_identity`         | Proyeksi identitas profil pegawai/platform dengan fallback username.     | `display_name`, `identity_source`, `contact_email`, `whatsapp`, `employee_id`.                                       |
| `roles`                   | Role dasar platform/organisasi/self.                                     | `code`, `name`, `scope`, `description`, `is_system`.                                                                 |
| `permissions`             | Hak granular backend.                                                    | `code`, `description`.                                                                                               |
| `role_permissions`        | Mapping role ke permission.                                              | `role_id`, `permission_id`.                                                                                          |
| `user_organization_roles` | Role user per organisasi atau platform.                                  | `user_id`, `organization_id`, `role_id`, `location_scope_mode`, `active_from`, `active_until`, `created_by_user_id`. |
| `user_location_scopes`    | Batas data lokasi untuk Admin/Pimpinan; bukan sumber penempatan pegawai. | `user_organization_role_id`, `organization_id`, `location_id`.                                                       |

Schema final menanam seluruh permission modul pegawai, penempatan, kontrak,
disiplin, akun, import, file privat, dan profil mandiri. Superadmin dan HRD mendapat
permission pengelolaan; Pimpinan hanya mendapat permission baca yang ditetapkan;
Pegawai hanya mendapat permission `*_self`. Superadmin memakai use case lintas
organisasi yang eksplisit, sedangkan HRD tetap dibatasi ke organisasi dari session.
Jalankan `npm run db:check` setelah setup atau upgrade database untuk memastikan
seluruh relasi, kolom, constraint, dan mapping permission tersedia.

## Pegawai dan Profil

| Tabel                         | Fungsi                                                                  | Kolom Kunci                                                                                                                                                                                                                                   |
| ----------------------------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `employees`                   | Profil inti pegawai dan penghubung akun self-service melalui `user_id`. | `organization_id`, `employee_no`, `user_id`, `full_name`, `preferred_name`, `national_id`, data lahir, `gender`, `religion`, `marital_status`, `employment_status`, `joined_date`, `termination_date`, `profile_photo_file_id`, `deleted_at`. |
| `employee_contacts`           | Kontak dan alamat pegawai.                                              | `employee_id`, email pribadi, `whatsapp` E.164 `+628...`, alamat KTP/domisili, wilayah, `postal_code`. Kolom email/telepon lama tetap tersedia untuk kompatibilitas tetapi tidak dipakai workflow pegawai saat ini.                           |
| `employee_identifiers`        | KK, BPJS, NPWP, dan identitas administratif lain.                       | `employee_id`, `identifier_type`, `identifier_label`, `identifier_value`, `issued_at`, `expires_at`, `is_verified`, `document_file_id`. KTP tetap memakai `employees.national_id` yang wajib berisi 16 digit; UI tidak menawarkan paspor.     |
| `employee_bank_accounts`      | Rekening pegawai sensitif.                                              | `employee_id`, `bank_name`, `account_number`, `account_holder`, `is_primary`, `verified_at`.                                                                                                                                                  |
| `employee_dependents`         | Tanggungan/keluarga.                                                    | `employee_id`, `relationship`, `full_name`, `birth_date`, `national_id`, `phone`, `is_dependent`, `is_emergency_contact`.                                                                                                                     |
| `employee_emergency_contacts` | Kontak darurat.                                                         | `employee_id`, `full_name`, `relationship`, `phone`, `address`, `is_primary`.                                                                                                                                                                 |
| `employee_social_accounts`    | Akun sosial bila diperlukan.                                            | `employee_id`, `platform`, `handle_or_url`.                                                                                                                                                                                                   |
| `employee_educations`         | Riwayat pendidikan.                                                     | `employee_id`, `education_level`, `institution`, `field_of_study`, `graduation_year`, `is_highest`, `certificate_file_id`.                                                                                                                    |
| `employee_skills`             | Keahlian pegawai.                                                       | `employee_id`, `skill_name`, `proficiency_level`, `notes`.                                                                                                                                                                                    |
| `employee_certifications`     | Sertifikasi profesional.                                                | `employee_id`, `certification_name`, `issuer`, `credential_no`, `issued_at`, `expires_at`, `certificate_file_id`.                                                                                                                             |
| `employee_documents`          | Dokumen privat pegawai.                                                 | `employee_id`, `document_type`, `file_id`, `issued_at`, `expires_at`, `verified_at`, `verified_by_user_id`.                                                                                                                                   |
| `employee_import_batches`     | Header, progres per pegawai, dan idempotensi import Excel.              | `source_file_id`, `status`, hitungan baris/pegawai, `committed_employees`, `created_by_user_id`, `validated_at`, `committed_at`, `error_summary`.                                                                                             |
| `employee_import_rows`        | Preview lintas-sheet dan error terstruktur sebelum commit.              | `batch_id`, `sheet_name`, `row_number`, `entity_type`, `entity_ref`, `employee_no`, `raw_data`, `normalized_data`, `validation_errors`, `status`, `employee_id`.                                                                              |

Lifecycle akhir hubungan kerja memakai `employees.employment_status` dengan nilai final `terminated`, `retired`, atau `deceased`, disertai `termination_date` dan `termination_reason`. Workflow aplikasi hanya menerima tanggal sejak `joined_date` sampai hari ini, menutup penempatan serta kontrak aktif, menonaktifkan akun tertaut, dan menulis `audit_logs` dalam satu transaksi. Profil, dokumen, disiplin, kontrak, dan penempatan historis tidak dihapus. Setelah status final tersimpan, perubahan profil atau histori operasional melalui form biasa ditolak.

Import pegawai memakai workbook `.xlsx` multi-sheet resmi tanpa foto, dokumen, kasus disiplin, atau tindakan sanksi. NIK wajib 16 digit untuk import. Nomor baris unik di dalam sheet melalui `(batch_id,sheet_name,row_number)`. Validasi dikelompokkan berdasarkan `employee_no`, dan commit atomik per pegawai menghasilkan status `committed` atau `partially_committed` tanpa menggagalkan pegawai valid lain. Detail operasional berada di `docs/employee-import.md`.

## Penyimpanan Privat Pegawai

Development memakai `UPLOAD_ROOT` atau default `<project>/uploads`. Object key menggunakan `org_{organizationId}/pegawai/employee_{employeeId}/{kategori}/{tahun}/{uuid}.{ext}`; nama, NIK, rekening, dan data pribadi tidak boleh berada pada path. Browser hanya mengakses `GET /api/uploads/:fileId`, dan route selalu memeriksa session, permission, organisasi, scope pegawai, audit, `Cache-Control: private, no-store`, serta `nosniff`. File sumber import berada pada `org_{organizationId}/imports/pegawai/{tahun}/{uuid}.xlsx`.

UI membaca kelengkapan melalui `GET /api/employees/:id/documents`, yang menyusun checklist Pas foto, KTP, KK, NPWP, BPJS Kesehatan, BPJS Ketenagakerjaan, kontrak, SK penempatan, ijazah, sertifikasi, dan identitas lain dari relasi domain. Endpoint checklist hanya mengembalikan status, jumlah, dan tanggal pembaruan terakhir. Metadata serta file dibuka dari konteks domain terkait melalui endpoint berbasis file ID; `object_key` tetap tidak dikirim ke browser.

## Kontrak dan Penempatan

| Tabel                        | Fungsi                                                                                                                                                                                                                                                                                                                   | Kolom Kunci                                                                                                                                                                                                          |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `employment_types`           | Jenis hubungan kerja per organisasi.                                                                                                                                                                                                                                                                                     | `organization_id`, `code`, `name`, `requires_end_date`, `is_active`, `created_at`, `updated_at`.                                                                                                                     |
| `employment_contracts`       | Histori kontrak pegawai. Kontrak baru menutup kontrak aktif sehari sebelum tanggal mulai baru dan menandainya `renewed`; salah input dapat dikoreksi dengan version check atau dibatalkan secara logis, tetapi record tidak dihapus. Alasan pembatalan ditampilkan pada histori kontrak agar keputusan dapat ditelusuri. | `employee_id`, `employment_type_id`, `contract_no`, `start_date`, `end_date`, `status`, `document_file_id`, `notes`, `updated_at`, `cancelled_at`, `cancellation_reason`, `cancelled_by_user_id`.                    |
| `employee_assignments`       | Histori dan sumber penempatan aktif pegawai untuk validasi lokasi/divisi. Salah input dapat dikoreksi dengan version check dan audit tanpa menghapus record historis.                                                                                                                                                    | `employee_id`, `location_id`, `organization_unit_id`, `position_id`, `supervisor_employee_id`, `assignment_type`, `change_type`, `effective_from`, `effective_until`, `decree_no`, `document_file_id`, `updated_at`. |
| `employee_onboarding_drafts` | Draft privat wizard pegawai selama tujuh hari.                                                                                                                                                                                                                                                                           | `organization_id`, `created_by_user_id`, `status`, `current_step`, `payload`, `version`, `submitted_employee_id`, `expires_at`, `updated_at`.                                                                        |

`employee_onboarding_drafts` hanya menyimpan satu draft aktif per actor dan organisasi. Penyimpanan checkpoint menggunakan `version` untuk optimistic concurrency. Finalisasi mengunci draft, membuat pegawai, kontak, kontrak, dan penempatan awal secara atomik, lalu menghubungkan file staging kepada pegawai. Pas foto kategori `employee_photo` bersifat opsional dan ditautkan melalui `employees.profile_photo_file_id`; foto dapat diunggah saat onboarding maupun dari Identitas administratif pada Kelola Profil Lengkap. KTP kategori `identity` juga opsional dan dicatat sebagai `employee_documents.document_type='ktp'`. Pada onboarding, nomor dan PDF kontrak awal boleh dilengkapi kemudian. Tanggal akhir hanya diminta ketika `employment_types.requires_end_date=true`. Penerbitan kontrak resmi melalui lifecycle kontrak tetap mewajibkan nomor dan `document_file_id` kategori `contract`; penempatan baru melalui lifecycle resmi tetap meminta nomor SK dan `document_file_id` kategori `assignment_decree`. Pada onboarding dan koreksi salah input, kedua data tersebut boleh kosong atau dilepas secara eksplisit; file histori tetap dipertahankan dan perubahan dicatat pada audit.

Koreksi atau pembatalan kontrak menggunakan `PATCH/DELETE /api/employees/:employeeId/contracts/:contractId`. `DELETE` bersifat pembatalan logis, wajib menerima alasan dan versi record, serta tidak menghapus dokumen maupun histori audit. Histori kontrak menggabungkan `created_at`, metadata pembatalan, identitas terpusat pengguna, dan `audit_logs` untuk menampilkan pelaku serta waktu pencatatan, koreksi terakhir, dan pembatalan. Kontrak awal yang dibuat bersama pegawai menggunakan audit `employee.create` sebagai fallback pembuat. Dokumen kontrak dan SK dikembalikan pada histori masing-masing dan masing-masing record hanya menyediakan satu aksi dokumen agar tidak ada kontrol berbeda dengan perilaku sama.

Koreksi salah input penempatan menggunakan `PATCH /api/employees/:employeeId/assignments/:assignmentId` dengan `updated_at` sebagai version check. Koreksi memvalidasi cakupan lokasi, master organisasi, dokumen SK bila disertakan, serta benturan periode penempatan utama; perubahan dicatat pada `audit_logs` dan tidak menghapus record histori. Penempatan aktif tetap ditutup melalui workflow Penempatan baru agar urutan lifecycle tidak rusak.

## Shift, Jadwal, dan Absensi

| Tabel                          | Fungsi                                          | Kolom Kunci                                                                                                                                                                         |
| ------------------------------ | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `work_shifts`                  | Master shift fixed/flexible/field/off.          | `shift_type`, `start_time`, `end_time`, window clock-in, `required_work_minutes`, `break_minutes`, `crosses_midnight`, toleransi terlambat/pulang awal, lembur kandidat.            |
| `shift_patterns`               | Pola mingguan/rotasi.                           | `code`, `name`, `cycle_days`, `is_active`.                                                                                                                                          |
| `shift_pattern_days`           | Isi harian pola shift.                          | `shift_pattern_id`, `day_no`, `shift_id`.                                                                                                                                           |
| `shift_assignments`            | Aturan pola untuk pegawai/unit/lokasi.          | target pegawai/unit/lokasi, `shift_pattern_id`, `effective_from`, `effective_until`, `priority`, `created_by_user_id`.                                                              |
| `employee_daily_schedules`     | Snapshot jadwal harian.                         | `employee_id`, `work_date`, `shift_id`, `location_id`, `scheduled_start_at`, `scheduled_end_at`, `status`, `is_override`, `override_reason`.                                        |
| `attendance_points`            | Titik/geofence absensi.                         | `location_id`, `code`, `name`, `latitude`, `longitude`, `radius_m`, `max_accuracy_m`, flags foto/liveness/background, masa aktif.                                                   |
| `attendance_point_assignments` | Aturan titik absensi untuk pegawai/unit/lokasi. | `attendance_point_id`, target pegawai/unit/lokasi, `effective_from`, `effective_until`.                                                                                             |
| `attendance_devices`           | Registry perangkat/sumber absensi.              | `device_code`, `name`, `device_type`, `location_id`, `api_key_hash`, `is_active`, `last_seen_at`.                                                                                   |
| `attendance_import_batches`    | Header import Excel/CSV.                        | `source_file_id`, `status`, hitungan baris, `uploaded_by_user_id`, `committed_at`, `error_summary`.                                                                                 |
| `attendance_import_rows`       | Staging baris import.                           | `batch_id`, `row_no`, `employee_no`, `employee_id`, `occurred_at`, `event_type`, `raw_data`, `validation_status`, `validation_errors`.                                              |
| `attendance_event_receipts`    | Idempotensi mobile/web.                         | `client_event_id`, `employee_id`, `event_date`, `received_at`, `processing_status`, `rejection_code`.                                                                               |
| `attendance_events`            | Event absensi mentah partisi bulanan.           | `event_date`, `employee_id`, `receipt_id`, `daily_schedule_id`, `attendance_point_id`, `device_id`, `event_type`, `occurred_at`, `source`, koordinat, validasi, foto, offline flag. |
| `attendance_daily_summaries`   | Rekap cepat dashboard.                          | `employee_id`, `work_date`, clock-in/out pertama/akhir, menit kerja/terlambat/pulang awal/lembur, `attendance_status`, versi kalkulasi, override HRD.                               |

## Cuti, Izin, dan Disiplin

| Tabel                        | Fungsi                                                                                                 | Kolom Kunci                                                                                                                                                            |
| ---------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `leave_types`                | Master kebijakan cuti/izin per organisasi.                                                             | `code`, `name`, `category`, `unit`, `requires_attachment`, `required_attachment_category`, `uses_balance`, `annual_allowance`, `is_active`, `updated_at`.              |
| `leave_requests`             | Pengajuan atau pencatatan HRD yang menyimpan periode, keputusan, dan pembatalan tanpa menimpa histori. | `request_no`, `employee_id`, `leave_type_id`, `start_at`, `end_at`, `requested_units`, `reason`, `submission_source`, `status`, `cancelled_at`, `cancellation_reason`. |
| `leave_entitlements`         | Periode hak saldo pegawai untuk satu jenis dan tahun kalender.                                         | `employee_id`, `leave_type_id`, `period_start`, `period_end`, `created_by_user_id`.                                                                                    |
| `leave_balance_transactions` | Ledger append-only untuk grant, carry-over, penyesuaian, pemakaian, dan pengembalian saldo.            | `entitlement_id`, `leave_request_id`, `transaction_type`, `units`, `reason`, `created_by_user_id`.                                                                     |
| `leave_request_attachments`  | Lampiran izin/cuti.                                                                                    | `leave_request_id`, `file_id`, `attachment_category`, `uploaded_at`.                                                                                                   |
| `leave_decisions`            | Keputusan final HRD.                                                                                   | `leave_request_id`, `decision`, `decided_by_user_id`, `decision_role`, `notes`, `decided_at`.                                                                          |

Jatah tahunan, durasi pencatatan, dan seluruh transaksi saldo cuti/izin menggunakan bilangan bulat. Sistem tidak menerima atau membulatkan pecahan hari/jam secara diam-diam.

| Tabel                   | Fungsi                                                                                                                                                                                                                           | Kolom Kunci                                                                                                                                                                                                                             |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `discipline_rules`      | Aturan pembentuk indikator.                                                                                                                                                                                                      | `code`, `name`, `severity`, `metric_type`, `threshold_value`, `window_days`, `legal_reference`, `recommended_action`, `is_active`.                                                                                                      |
| `discipline_indicators` | Sinyal otomatis untuk ditinjau HRD.                                                                                                                                                                                              | `employee_id`, `rule_id`, periode, `measured_value`, `status`, `evidence`, `reviewed_by_user_id`, `reviewed_at`.                                                                                                                        |
| `discipline_cases`      | Pemeriksaan kasus oleh HRD.                                                                                                                                                                                                      | `case_no`, `employee_id`, `indicator_id`, `severity`, `incident_date`, `description`, `employee_explanation`, `status`, `opened_by_user_id`, `closed_at`.                                                                               |
| `disciplinary_actions`  | Tindakan/sanksi resmi, satu tindakan per kasus. Draft dapat diedit dan hanya terlihat oleh HRD/Superadmin; Pimpinan hanya menerima tindakan yang sudah resmi. Tindakan aktif hanya dicabut secara logis agar histori tetap utuh. | `discipline_case_id`, `employee_id`, `action_type`, `letter_no`, tanggal berlaku, `status`, `direct_escalation`, `escalation_reason`, `document_file_id`, `issued_by_user_id`, `revoked_at`, `revoked_by_user_id`, `revocation_reason`. |

## Audit, Outbox, dan View

| Objek                          | Fungsi                                            | Kolom Kunci                                                                                                                                            |
| ------------------------------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `audit_logs`                   | Audit append-only.                                | `organization_id`, `actor_user_id`, `action`, `entity_type`, `entity_id`, data sebelum/sesudah tersaring, IP, user agent, `request_id`, `occurred_at`. |
| `integration_outbox`           | Transactional outbox.                             | `organization_id`, `event_type`, `aggregate_type`, `aggregate_id`, `payload`, `published_at`, `attempts`, `next_attempt_at`, `last_error`.             |
| `v_employee_current_profile`   | View profil dan penempatan aktif untuk dashboard. | Baca profil pegawai aktif dari `employees` + `employee_assignments` aktif.                                                                             |
| `v_employee_attention_summary` | View ringkasan perhatian 30 hari.                 | Ringkasan absensi/indikator untuk HRD dan pimpinan tanpa membuka bukti sensitif.                                                                       |

## Kapan Membuka SQL

- Membuat migration baru atau memvalidasi constraint/FK/index.
- Mengecek enum `CHECK` secara persis.
- Mengecek definisi partition `attendance_events`.
- Mengecek definisi view atau fungsi PostgreSQL.
- Menjalankan query plan atau debugging database sungguhan.
